import 'package:flutter/material.dart';

class BottamSheetWidget extends StatelessWidget {
  const BottamSheetWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
